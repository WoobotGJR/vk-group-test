// by default vite starts server on 5173
const PORT = '5173';

// absolute url to groups.json on vite server
const url = `http://localhost:${PORT}/src/utils/groups.json`;

const AVATAR_SIZE = 100;

export { url, AVATAR_SIZE };
